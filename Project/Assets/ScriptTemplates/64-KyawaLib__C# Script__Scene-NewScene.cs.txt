using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;
using KyawaLib;

#ROOTNAMESPACEBEGIN#
public class #SCRIPTNAME#Manager : SingletonClass<#SCRIPTNAME#Manager>
{
    #SCRIPTNAME# m_gameObject = null;

    public void LoadScene()
    {
        SceneLoader.instance.LoadSceneAsync("#SCRIPTNAME#", LoadSceneMode.Single,
            onLoadedAction: () => m_gameObject = new GameObject("**#SCRIPTNAME#**").AddComponent<#SCRIPTNAME#>());
    }
}

public class #SCRIPTNAME# : MonoBehaviour
{
    IEnumerator Start()
    {
        var corouines = new List<IEnumerator>() { CoEnter(), CoMain(), CoExit() };
        foreach (var co in corouines)
            yield return co;
    }

    IEnumerator CoEnter()
    {
        yield return null;
    }

    IEnumerator CoMain()
    {
        yield return null;
    }

    IEnumerator CoExit()
    {
        yield return null;
    }
}
#ROOTNAMESPACEEND#